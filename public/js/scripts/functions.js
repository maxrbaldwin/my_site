var app = (function() {
    var semantic_events = {

    };
    var _score = {
        load: function(tabs) {
            $(tabs).each(function(i){

            });
        }
    };
    var template = {
        get_template: function() {

        },
        load_template: function() {

        }
    };

    return {
        _score: score,
        temp: template,
        s_events: semantic_events
    };
})();
